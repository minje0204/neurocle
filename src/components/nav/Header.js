import styled from "styled-components";
import Nav from "./Nav";

const Header = () => {
  return (
    <HeaderWrapper>
      <Nav />
    </HeaderWrapper>
  );
};

export default Header;

const HeaderWrapper = styled.div``;
